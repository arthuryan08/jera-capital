# Prompt 005 - CLAUDE.md Adjustments

**Date**: 2026-04-09

---

I need you to make adjustments to CLAUDE.md. Read the current file first and then apply these changes:

## 1. Simplify "Claude Code Behavior Instructions"
Remove generic instructions that you already know (such as "Always use TypeScript strict mode", "Read the file first before making changes", "Don't add comments to code you didn't change", "Don't refactor unrelated code"). Maintain ONLY the specific rules of this project:
- Engine functions must be pure (zero imports from db/, modules/, external services)
- The tax example from business rules MUST be a test case
- UI labels in Brazilian Portuguese, code in English
- Currency formatting with Intl.NumberFormat("pt-BR")
- Components in ui/ are auto-generated by shadcn, never edit manually
- Conventional commits: feat:, fix:, test:, docs:, refactor:, chore:

## 2. Remove "Dependencies" section
Delete the backend and frontend dependency block. This will be in package.json.

## 3. Add "Auth Flow" section
Add a section explaining the complete flow right after "Architecture Principles":

### Auth Flow
1. Backend exposes `POST /api/auth/login` which validates email/password with bcrypt and returns a signed JWT with the `JWT_SECRET` envelope.
2. Frontend uses NextAuth v5 with CredentialsProvider which calls the backend login endpoint.
3. The JWT returned by the backend is stored in the NextAuth session via callbacks (jwt + session).
4. Requests from the frontend to the backend pass through the API proxy (`/api/proxy/[...path]`) which injects the JWT into the Authorization header.
5. Backend validates the JWT on all protected routes via the @fastify/jwt plugin.
6. The same `JWT_SECRET` must be shared between the backend and NextAuth.

## 4. Add "Seed Data" section
Add after "Database Schema":

### Seed Data
In the initial migration, create a default user for the evaluator to test without needing to register:
- Email: admin@test.com
- Password: password123
- Name: Admin

## 5. Add "Ports" section
Add after "Tech Stack":

### Default Ports
- Frontend: 3000
- Backend: 3001
- Swagger UI: http://localhost:3001/docs

## 6. Add auth endpoint to the API table
Add to the endpoints table:
| POST | /api/auth/login | Login, returns JWT | No |
| POST | /api/auth/register | Create user account | No |

Nothing else changes in the file. Maintain the entire structure, business rules, examples, and everything that already exists.
